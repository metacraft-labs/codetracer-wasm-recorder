# Maintainer: Metacraft Labs <ci@metacraft-labs.com>
#
# Generated by codetracer-launcher/packaging/recorder-templates/generate.py
# from codetracer-wasm-recorder/packaging/recorder-metadata.yml.
# DO NOT edit this file by hand — re-run the generator instead.
#
# PKGBUILD for codetracer-wasm-recorder (CodeTracer recorder for WebAssembly modules (wazero engine)).

pkgname=codetracer-wasm-recorder
pkgver=0.1.0
pkgrel=1
pkgdesc="CodeTracer recorder for WebAssembly modules (wazero engine)"
arch=('x86_64' 'aarch64')
url="https://github.com/metacraft-labs/codetracer-wasm-recorder"
license=('Apache-2.0')
depends=('glibc')
makedepends=('rust' 'cargo')
provides=("codetracer-wasm-recorder")
source=("$pkgname-$pkgver.tar.gz::https://github.com/metacraft-labs/codetracer-wasm-recorder/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=('REPLACE_AT_PUBLISH_TIME_source')

build() {
  cd "$srcdir/codetracer-wasm-recorder-$pkgver"
  cargo build --release --locked --bin codetracer-wasm-recorder
}

check() {
  cd "$srcdir/codetracer-wasm-recorder-$pkgver"
  cargo test --release --locked --bin codetracer-wasm-recorder || true
}

package() {
  cd "$srcdir/codetracer-wasm-recorder-$pkgver"
  install -Dm755 "target/release/codetracer-wasm-recorder" \
    "$pkgdir/usr/bin/codetracer-wasm-recorder"
  if [[ -f LICENSE ]]; then
    install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
  fi
  if [[ -f README.md ]]; then
    install -Dm644 README.md "$pkgdir/usr/share/doc/$pkgname/README.md"
  fi
}
